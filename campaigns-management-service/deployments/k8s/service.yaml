apiVersion: v1
kind: Service
metadata:
  name: campaigns-management-service
  namespace: rules-engine
  labels:
    app: campaigns-management-service
spec:
  selector:
    app: campaigns-management-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

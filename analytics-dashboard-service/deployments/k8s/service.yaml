apiVersion: v1
kind: Service
metadata:
  name: analytics-dashboard-service
  namespace: rules-engine
  labels:
    app: analytics-dashboard-service
spec:
  selector:
    app: analytics-dashboard-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

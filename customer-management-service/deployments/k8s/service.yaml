apiVersion: v1
kind: Service
metadata:
  name: customer-management-service
  namespace: rules-engine
  labels:
    app: customer-management-service
spec:
  selector:
    app: customer-management-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
